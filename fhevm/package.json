{
  "name": "kaddu-fhevm",
  "version": "1.0.0",
  "description": "Kaddu — confidential civic contracts on Zama fhEVM: on-chain confidential voting (KadduVote) and a tamper-proof tontine whose governance is decided by confidential on-chain voting (KadduTontine).",
  "license": "BSD-3-Clause-Clear",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@fhevm/hardhat-plugin": "0.4.2",
    "@fhevm/solidity": "0.11.1",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "encrypted-types": "0.0.4",
    "hardhat": "^2.28.0"
  }
}
